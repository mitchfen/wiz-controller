@page "/"
@rendermode InteractiveServer

<PageTitle>WiZ Controller</PageTitle>

<div class="container">
    <div class="master-control">
        <label>All Lights</label>
        <div class="master-slider-container">
            <input type="range" min="0" max="100"
                   value="@_masterBrightness"
                   @onchange="(e) => SetAllBrightnessFromMaster(e)" />
        </div>
    </div>

    <div class="light-grid">
        @for (int i = 0; i < LightIps.Length; i++)
        {
            var ip = LightIps[i];
            var lightName = LightNames[i];

            <div class="card">
                <h5>@lightName</h5>
                
                <div class="brightness-control">
                    <label>Brightness</label>
                    <input type="range" min="0" max="100"
                           value="@_brightness.GetValueOrDefault(ip, 0)"
                           @onchange="(e) => ChangeBrightness(ip, e)" />
                </div>
            </div>
        }
    </div>
</div>
